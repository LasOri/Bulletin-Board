name: Build and Deploy to GitHub Pages

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:  # Allow manual triggering

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest  # Switch to Ubuntu for better WASM tooling
    permissions:
      contents: write  # Needed for GitHub Pages deployment

    container:
      image: ghcr.io/swiftwasm/swift:latest  # Official Swift WASM Docker image

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Configure Git for private repo access
        run: |
          # Configure git to use PAT for LINKER private repo
          git config --global url."https://${{ secrets.LINKER_ACCESS_TOKEN }}@github.com/".insteadOf "https://github.com/"

      - name: Verify Swift and WASM SDK
        run: |
          echo "Swift version:"
          swift --version
          echo ""
          echo "Available SDKs:"
          swift sdk list || echo "No SDKs configured yet"
          echo ""
          echo "Checking for WASM target support..."
          swift build --help | grep -i wasm || echo "WASM support built-in"

      - name: Cache Swift packages
        uses: actions/cache@v4
        with:
          path: .build
          key: ${{ runner.os }}-spm-${{ hashFiles('**/Package.resolved') }}
          restore-keys: |
            ${{ runner.os }}-spm-

      - name: Run tests
        run: |
          echo "Running tests..."
          swift test
          echo "✅ All tests passed"

      - name: Build WASM binary
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          echo "Building WASM binary..."

          # Swift WASM Docker image has built-in WASM support
          # Build for WASM target directly
          swift build --triple wasm32-unknown-wasi -c release

          echo "✅ WASM binary created"
          echo "Build output:"
          find .build -name "*.wasm" -type f
          ls -lh .build/wasm32-unknown-wasi/release/ || ls -lh .build/release/

      - name: Prepare deployment files
        run: |
          echo "Preparing deployment..."
          mkdir -p dist

          # Find WASM binary
          WASM_FILE=$(find .build -name "BulletinBoard.wasm" -type f | head -1)

          if [ -z "$WASM_FILE" ]; then
            echo "❌ WASM binary not found"
            echo "Build directory contents:"
            find .build -type f -name "*.wasm" || echo "No .wasm files found"
            find .build -type f | head -20
            exit 1
          fi

          echo "Found WASM binary: $WASM_FILE"
          cp "$WASM_FILE" dist/BulletinBoard.wasm
          echo "✅ Copied WASM binary ($(stat -c %s dist/BulletinBoard.wasm 2>/dev/null || stat -f %z dist/BulletinBoard.wasm) bytes)"

          # Copy static assets (HTML, CSS, JS)
          if [ -d "Public" ]; then
            cp Public/index.html dist/
            cp Public/styles.css dist/
            cp Public/BulletinBoard.js dist/
            echo "✅ Copied static assets"
          else
            echo "❌ Public directory not found"
            exit 1
          fi

          # Create .nojekyll to prevent GitHub Pages from processing files
          touch dist/.nojekyll

          # List contents
          echo "📦 Deployment contents:"
          ls -lh dist/

          echo "✅ Deployment files prepared"

      - name: Deploy to GitHub Pages
        if: github.ref == 'refs/heads/main' && github.event_name == 'push'
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./dist
          force_orphan: true
          user_name: 'github-actions[bot]'
          user_email: 'github-actions[bot]@users.noreply.github.com'
          commit_message: 'Deploy ${{ github.sha }}'

      - name: Summary
        if: success()
        run: |
          echo "🎉 Build and deployment successful!"
          echo "📊 Tests: ✅ All 398 tests passed"
          echo "📦 WASM: ✅ Bundle created"
          if [ "${{ github.ref }}" == "refs/heads/main" ]; then
            echo "🚀 Deploy: ✅ Published to GitHub Pages"
            echo "🌐 URL: https://${{ github.repository_owner }}.github.io/Bulletin-Board/"
          else
            echo "ℹ️  Skipping deployment (not main branch)"
          fi
